import { createTheme } from "@aws-amplify/ui-react";

export const alertTimeout = 300;
export const drawerTimeout = 200;
export const navIconGap = "0.35rem";

export const theme = createTheme({
  name: "app-theme",
  tokens: {
    components: {},
  },
});
