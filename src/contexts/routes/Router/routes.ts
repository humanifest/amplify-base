import { __ } from "@avaelus/super-project-utils";

const topLevelRoutes = {
  user: {
    ":userId": __,
  },
  about: __,
  contact: __,
  tos: __,
  privacy: __,
  admin: __,
};

export default topLevelRoutes;
